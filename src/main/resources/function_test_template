package alex.z.com.github.alexzelentsov.utils.functions;

 import org.junit.Test;

 import static org.hamcrest.core.Is.is;
 import static org.junit.Assert.assertThat;

 /**
  * Created by alexander.zelentsov on 24.07.2017.
  */
 public class TestFunction%N% {

     @Test
     public void apply() {
         Function%N%<%Integer%, Integer> function%N% = (%args%) -> %return%;
         Integer result = function%N%.apply(%test_params%);
         assertThat(result, is(%result%));
     }

 }